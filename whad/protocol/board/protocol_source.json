{
  "source": "../whad-protocol",
  "commit": "see ../whad-protocol HEAD",
  "generated_root": "../whad-protocol/dist/nanopb",
  "note": "Paths are relative to the whad-lib repo root in the standard 4-repo workspace layout (whad-protocol as sibling). Re-run whad-protocol `make` then `cp -r ../whad-protocol/dist/nanopb/whad/protocol/* whad/protocol/` to refresh."
}
